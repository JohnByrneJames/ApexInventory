@{ 
    // Executes before any view content; setting layout here allows DRY code. This one value changes the default view for the entire web app
    // * It is possible to overwrite the layout here by create anotehr _viewstart.cshtml and placing it at the root of a view collection *
    Layout = "_Layout";
}
